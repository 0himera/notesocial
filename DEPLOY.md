# 🚀 Деплой NoteMe на Vercel — Пошаговый гайд

## Шаг 1: Создайте аккаунт JSONBin.io (хранилище данных)

1. Перейдите на **https://jsonbin.io**
2. Нажмите **Sign Up** (можно войти через Google)
3. После входа нажмите **Create a Bin**
4. Вставьте начальные данные:
```json
{
  "users": []
}
```
5. Нажмите **Create**
6. **Скопируйте и сохраните:**
   - **Bin ID** — находится в URL после `/b/` (например: `67945a1234abcd5678ef`)
   - **API Key** — нажмите на иконку профиля → **API Keys** → скопируйте ключ

---

## Шаг 2: Создайте Git-репозиторий

### Вариант A: Через GitHub Desktop
1. Откройте GitHub Desktop
2. File → Add Local Repository → выберите папку `x:\webproj\noteme`
3. Если нет репозитория — нажмите "Create a Repository"
4. Имя: `noteme`
5. Нажмите **Publish repository** (можно сделать приватным)

### Вариант B: Через командную строку
```bash
cd x:\webproj\noteme
git init
git add .
git commit -m "Initial commit"
git branch -M main
git remote add origin https://github.com/ВАШ_USERNAME/noteme.git
git push -u origin main
```

---

## Шаг 3: Подключите к Vercel

1. Перейдите на **https://vercel.com**
2. Войдите через GitHub
3. Нажмите **Add New → Project**
4. Выберите репозиторий **noteme**
5. **Важно!** Перед деплоем добавьте Environment Variables:

### Environment Variables (обязательно!)

| Name | Value |
|------|-------|
| `JSONBIN_BIN_ID` | Ваш Bin ID из шага 1 |
| `JSONBIN_API_KEY` | Ваш API Key из шага 1 |

6. Нажмите **Deploy**

---

## Шаг 4: Готово! 🎉

После деплоя вы получите URL вида:
```
https://noteme-xxx.vercel.app
```

- **Главная:** `https://your-app.vercel.app/` — список юзеров
- **Админка:** `https://your-app.vercel.app/admin.html` — создание юзеров и заметок

---

## (Опционально) Шаг 5: Настройте Deploy Hook для авто-ребилда

Когда добавляется новая заметка, статика должна перегенерироваться:

1. В Vercel → ваш проект → **Settings** → **Git** → **Deploy Hooks**
2. Создайте хук с именем `rebuild`
3. Скопируйте URL хука
4. Добавьте новую Environment Variable:

| Name | Value |
|------|-------|
| `VERCEL_DEPLOY_HOOK` | URL хука |

5. **Redeploy** проект

Теперь при добавлении заметки страницы автоматически обновятся!

---

## Тестирование

1. Откройте `https://your-app.vercel.app/admin.html`
2. Создайте нового пользователя (вкладка "Новый юзер")
3. Запомните логин и пароль
4. Добавьте заметку (вкладка "Новая заметка")
5. Дождитесь ребилда (~30 секунд)
6. Откройте главную — увидите нового пользователя!

---

## Возможные проблемы

### "Заполните все поля"
→ Проверьте, что заполнены логин и пароль

### "Пользователь уже существует"
→ Выберите другой логин

### Заметки не появляются
→ Дождитесь ребилда (если настроен Deploy Hook) или вручную нажмите Redeploy в Vercel

### Ошибка 500
→ Проверьте Environment Variables в Vercel
